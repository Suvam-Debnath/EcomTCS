server:
  port: 8082

spring:
  application:
    name: product

  datasource:
    url: jdbc:postgresql://localhost:5432/productTCS
    username: postgres
    password: suvam123

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
